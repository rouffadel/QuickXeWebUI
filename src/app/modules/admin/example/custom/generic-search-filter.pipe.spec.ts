import { GenericSearchFilterPipe } from './generic-search-filter.pipe';

describe('GenericSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GenericSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
