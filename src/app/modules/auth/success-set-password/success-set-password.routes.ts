import { Routes } from '@angular/router';
import { SuccessSetPasswordComponent } from './success-set-password.component';

export default [
    {
        path: '',
        component: SuccessSetPasswordComponent,
    },
] as Routes;
